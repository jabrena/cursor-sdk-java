package info.jab.cursor.sdk;

/**
 * Enum representing the type of repository location for the Cursor agent.
 */
public enum RepositoryLocation {
    /**
     * Local repository location.
     */
    LOCAL,
    /**
     * Remote repository location.
     */
    REMOTE
}

